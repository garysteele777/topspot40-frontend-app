// src/lib/types/options.ts
// Thin compatibility layer during refactor
// Do NOT define types here — always define in playback.ts

export type {
  ModeType,
  Language,
  VoicePart,
  PlaybackOrder,
  PauseMode,
  VoicePlayMode,
  CategoryMode,
  LayoutMode
} from '$lib/types/playback';
